##############################
## POSTGRESQL BACKUP CONFIG ##
##############################

DUMP_DIR={{ dump_dir }}
DATABASES_LIST="{{ databases_list | join(',') }}"
DAYS_TO_KEEP={{ days_to_keep }}
